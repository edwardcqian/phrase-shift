# Anthropic API Key (required for translations)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API Key (required for TTS)
OPENAI_API_KEY=sk-...

# Database (defaults work with docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=phraseshift

# Backend server port (default: 8080)
PORT=8080
